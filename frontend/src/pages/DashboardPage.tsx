import React from 'react';

const DashboardPage: React.FC = () => {
  return (
    <div>
      <h2>Dashboard</h2>
      <p>Low stock, ready to expire, and expired products will be shown here.</p>
    </div>
  );
};

export default DashboardPage;